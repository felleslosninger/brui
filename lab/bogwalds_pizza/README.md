The demo defaults to the `envoy-default` inference target.

Set your own API key before starting the proxy:

```sh
export ENVOY_API_KEY=your-api-key
```

If you want to run against local Ollama instead, change `lab/bogwalds_pizza/src/config.json` to use `local-nano`.

npm install
npm run dev
