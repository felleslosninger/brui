{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "addPizza",
        "description": "Add a pizza to the order",
        "parameters": {
          "type": "object",
          "properties": {
            "pizzaName": {
              "type": "string",
              "description": "Name of the pizza to add",
              "enum": ["Pasta Bolognese", "Pasta Bolognese without bacon", "Pasta Bolognese without pasta"]
            }
          },
          "required": ["pizzaName"]
        }
      }
    },
    {
      "type": "function",
      "function": {
        "name": "changeActiveTab",
        "description": "Change tab to the given tab name",
        "parameters": {
          "type": "object",
          "properties": {
            "value": {
              "type": "string",
              "description": "Name of the tab to switch to",
              "enum": ["home", "menu", "order"]
            }
          },
          "required": ["value"]
        }
      }
    }
  ],
  "actions": [
    {
      "name": "addPizzaToOrder",
      "type": "function",
      "args": []
    },
    {
      "name": "setActiveTab",
      "type": "function",
      "args": ["value"]
    }
  ],
  "decisions": [
    { "tool": "addPizza", "actions": ["addPizzaToOrder"] },
    { "tool": "changeActiveTab", "actions": ["setActiveTab"] }
  ],
  "inference": {
    "kind": "inference",
    "name": "default",
    "spec": {
      "name": "local-nano",
      "provider": "ollama"
    }
  }
}
    